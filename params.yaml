project:
  random_seed: 42
  phase: "minimum_reproducible_loop"

paths:
  raw_dir: data/raw
  processed_dir: data/processed

preprocessing:
  enabled: false
  method: null

model:
  name: null

evaluation:
  metrics: []
